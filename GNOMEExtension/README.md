# GNOME Shell Extension - Расписание ЧувГУ

Расширение для GNOME Shell, показывающее расписание занятий на верхней панели

## 📋 Требования

- GNOME Shell 40+ (Ubuntu 21.04+, Fedora 34+, и др.)
- Запущенный TimetableWidget Backend API на `http://localhost:5000`

## 🚀 Установка

### Автоматическая установка (рекомендуется)

```bash
cd GNOMEExtension
chmod +x install.sh
./install.sh
```

### Перезапуск GNOME Shell

**X11 (Xorg):**
```bash
# Нажмите Alt+F2, введите 'r', нажмите Enter
# Или:
killall -3 gnome-shell
```

**Wayland:**
```bash
# Выйдите и войдите заново
```

### Включение расширения

```bash
gnome-extensions enable timetable@chuvsu.extensions.gnome.org
```

Или через **GNOME Extensions** приложение.

## 📦 Запуск Backend

```bash
cd ../TimetableWidget.Backend
dotnet run
```

Backend должен работать на `http://localhost:5000`

## 🔧 Использование

### Первый запуск (Авторизация)

1. **Кликните на иконку "ЧувГУ"** на верхней панели
2. В выпадающем меню нажмите **"Войти"**
3. Введите **email** и **пароль** от личного кабинета ЧувГУ
4. Нажмите **"Войти"**

После успешной авторизации расписание загрузится автоматически!

### Панельный индикатор

На панели отображается:
- 📅 Иконка календаря
- 📊 Количество пар (например: "3 пары")

### Popup меню

Кликните на индикатор, чтобы увидеть:
- 📋 **Заголовок** - "Расписание на сегодня/завтра"
- 🗓️ **День недели**
- 📚 **До 5 пар** с информацией:
  - Название дисциплины
  - Время (начало - конец)
  - Аудитория
  - Тип занятия
- 🔄 **Кнопки** - Войти/Обновить/Выйти
- 📊 **Статус** - состояние подключения

### Настройки

**Открыть настройки:**
```bash
gnome-extensions prefs timetable@chuvsu.extensions.gnome.org
```

Или: **ПКМ на индикаторе** → **"Настройки"** (в некоторых версиях)

Доступные настройки:

#### Подключение
- **URL бэкенда** - адрес API (по умолчанию `http://localhost:5000`)

#### Расписание
- **Подгруппа** - фильтр по подгруппе (0 = все подгруппы)

#### Обновление
- **Интервал** - как часто обновлять расписание (60-3600 секунд)

### Обновление расписания

- **Автоматически**: каждые 5 минут (по умолчанию)
- **Вручную**: клик на индикатор → "Обновить"

## 📁 Структура файлов

```
timetable@chuvsu.extensions.gnome.org/
├── extension.js            # Главный код расширения
├── prefs.js                # UI настроек
├── metadata.json           # Метаданные
├── stylesheet.css          # Стили
└── schemas/
    └── org.gnome.shell.extensions.chuvsu-timetable.gschema.xml
```

## 🔧 Управление расширением

### Включить/Выключить

```bash
# Включить
gnome-extensions enable timetable@chuvsu.extensions.gnome.org

# Выключить
gnome-extensions disable timetable@chuvsu.extensions.gnome.org
```

### Проверить статус

```bash
gnome-extensions info timetable@chuvsu.extensions.gnome.org
```

### Просмотр логов

```bash
# Логи GNOME Shell
journalctl -f -o cat /usr/bin/gnome-shell

# Или в реальном времени
journalctl -f | grep timetable
```

### Обновление

```bash
cd GNOMEExtension
./install.sh
# Перезапустите GNOME Shell (Alt+F2 → 'r')
```

### Удаление

```bash
gnome-extensions uninstall timetable@chuvsu.extensions.gnome.org
```

## 🐛 Решение проблем

### Расширение не появляется в списке

```bash
# Убедитесь что расширение установлено
ls ~/.local/share/gnome-shell/extensions/

# Перезапустите GNOME Shell
# X11: Alt+F2 → 'r'
# Wayland: выйти и войти
```

### "Backend недоступен"

- Убедитесь что backend запущен: `curl http://localhost:5000/`
- Проверьте URL в настройках
- Проверьте firewall

### Ошибка компиляции схем

```bash
# Перекомпилировать схемы вручную
cd ~/.local/share/gnome-shell/extensions/timetable@chuvsu.extensions.gnome.org
glib-compile-schemas schemas/
```

### Расширение не работает после обновления GNOME

Проверьте совместимость в `metadata.json`. Добавьте вашу версию GNOME в `shell-version`.

### Пустое расписание

- Проверьте настройку **Подгруппа** (попробуйте поставить 0)
- Убедитесь что вы авторизованы
- Обновите вручную (клик → "Обновить")

## 🎨 Кастомизация

### Изменить положение на панели

Расширение добавляется справа. Для изменения нужно редактировать `extension.js`:

```javascript
// Изменить позицию:
Main.panel.addToStatusArea(this.uuid, this._indicator, 0, 'right');
// 0 = индекс, 'right' можно заменить на 'center' или 'left'
```

### Изменить стили

Отредактируйте `stylesheet.css` и перезапустите GNOME Shell.

## 🔐 Безопасность

- Пароль **не хранится** в расширении
- Токен хранится в backend в зашифрованном виде
- Все данные передаются через localhost

## 📝 API Backend

Расширение общается с backend через REST API:

- `POST /api/auth/login` - Авторизация
- `GET /api/auth/status` - Проверка статуса
- `GET /api/timetable/today` - Расписание на сегодня

## 🌐 Совместимость

Протестировано на:
- GNOME 40 (Ubuntu 21.04)
- GNOME 41 (Fedora 35)
- GNOME 42 (Ubuntu 22.04)
- GNOME 43 (Fedora 37)
- GNOME 44 (Ubuntu 23.04)
- GNOME 45 (Fedora 39)
- GNOME 46 (Ubuntu 24.04)

## 👥 Авторы

- **Код виджета**: Казаков Кирилл Валерьевич, КТ-31-21
- **API расписания**: Петрянкин Даниил Евгеньевич, КТ-31-21
- **Дизайн**: Гаврилов Александр Сергеевич, КТ-42-20

## 📄 Лицензия

См. соглашения ЧувГУ:
- [Пользовательское соглашение](https://online.chuvsu.ru/doc/user_agreement)
- [Политика конфиденциальности](https://online.chuvsu.ru/doc/privacy_policy)

## 🆘 Поддержка

При проблемах:
1. Проверьте что backend запущен
2. Проверьте логи: `journalctl -f | grep timetable`
3. Переустановите расширение: `./install.sh`
4. Перезапустите GNOME Shell
