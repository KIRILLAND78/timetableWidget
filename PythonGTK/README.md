# Python GTK Timetable Widget - Расписание ЧувГУ

Универсальный виджет расписания для Linux, работающий на любом Desktop Environment

## 📋 Требования

- Python 3.7+
- GTK 3.0
- PyGObject (python3-gi)
- Requests
- Запущенный TimetableWidget Backend API на `http://localhost:5000`

## 🎯 Поддерживаемые DE

Работает на **ВСЕХ** Linux Desktop Environments:
- ✅ XFCE
- ✅ MATE
- ✅ LXQt
- ✅ LXDE
- ✅ Cinnamon (как альтернатива desklet)
- ✅ KDE Plasma (как альтернатива plasmoid)
- ✅ GNOME (как альтернатива extension)
- ✅ Budgie
- ✅ Pantheon
- ✅ И любой другой!

## 🚀 Установка

### Автоматическая установка (рекомендуется)

```bash
cd PythonGTK
chmod +x install.sh
./install.sh
```

### Ручная установка

#### 1. Установить зависимости

**Ubuntu/Debian:**
```bash
sudo apt install python3 python3-pip python3-gi python3-gi-cairo gir1.2-gtk-3.0
pip3 install --user requests
```

**Fedora:**
```bash
sudo dnf install python3 python3-pip python3-gobject gtk3
pip3 install --user requests
```

**Arch Linux:**
```bash
sudo pacman -S python python-pip python-gobject gtk3
pip3 install --user requests
```

#### 2. Установить приложение

```bash
# Установить зависимости
pip3 install --user -r requirements.txt

# Скопировать файлы
sudo mkdir -p /usr/local/lib/timetable-widget
sudo cp -r timetable_widget /usr/local/lib/timetable-widget/
sudo cp main.py /usr/local/lib/timetable-widget/

# Создать launcher
sudo cp install.sh launcher script manually (see install.sh)

# Установить desktop entry
mkdir -p ~/.local/share/applications
cp timetable-widget.desktop ~/.local/share/applications/
```

## 📦 Запуск Backend

```bash
cd ../TimetableWidget.Backend
dotnet run
```

Backend должен работать на `http://localhost:5000`

## 🎮 Запуск приложения

### Из терминала:
```bash
timetable-widget
```

### Из меню приложений:
Найдите **"ЧувГУ Расписание"** в меню приложений

### Автозапуск:
```bash
mkdir -p ~/.config/autostart
cp ~/.local/share/applications/timetable-widget.desktop ~/.config/autostart/
```

## 🔧 Использование

### Первый запуск (Авторизация)

1. Запустите приложение
2. Нажмите кнопку **"Войти"**
3. Введите **email** и **пароль** от личного кабинета ЧувГУ
4. Нажмите **"OK"**

После успешной авторизации расписание загрузится автоматически!

### Главное окно

Виджет отображается как frameless окно на рабочем столе:
- 📅 **Заголовок** - "Расписание на сегодня/завтра"
- 🗓️ **День недели**
- 📚 **До 5 пар** с информацией:
  - Название дисциплины
  - Время (начало - конец)
  - Аудитория
  - Тип занятия
- 🔄 **Кнопки**:
  - **Войти** - авторизация (или Обновить если уже вошли)
  - **Настройки** - открыть настройки
  - **Выйти** - выход из системы
- 📊 **Статус** - состояние подключения

### Перемещение виджета

**Просто перетащите** окно мышью в любое место на рабочем столе!

### Настройки

Нажмите кнопку **"Настройки"** для изменения:

#### Подключение
- **URL бэкенда** - адрес API (по умолчанию `http://localhost:5000`)

#### Расписание
- **Подгруппа** - фильтр по подгруппе (0 = все подгруппы)

#### Внешний вид
- **Прозрачность** - от 0% (невидимый) до 100% (непрозрачный)

#### Обновление
- **Интервал** - как часто обновлять расписание (60-3600 секунд)

### Обновление расписания

- **Автоматически**: каждые 5 минут (по умолчанию)
- **Вручную**: кнопка "Обновить"

## 📁 Структура файлов

```
PythonGTK/
├── main.py                      # Точка входа
├── requirements.txt             # Python зависимости
├── timetable-widget.desktop     # Desktop entry
├── install.sh                   # Скрипт установки
├── README.md                    # Эта документация
└── timetable_widget/            # Пакет приложения
    ├── __init__.py
    ├── window.py                # Главное окно (GTK)
    ├── api_client.py            # API клиент
    └── config.py                # Менеджер конфигурации
```

## 🔧 Конфигурация

Конфигурация хранится в:
```
~/.config/timetable-widget/config.json
```

Пример:
```json
{
  "backend_url": "http://localhost:5000",
  "x": 100,
  "y": 100,
  "width": 400,
  "height": 500,
  "group": 0,
  "transparency": 0.95,
  "update_interval": 300
}
```

## 🐛 Решение проблем

### Приложение не запускается

```bash
# Проверьте зависимости
python3 -c "import gi; gi.require_version('Gtk', '3.0'); from gi.repository import Gtk; print('GTK OK')"

# Проверьте requests
python3 -c "import requests; print('Requests OK')"
```

### "Backend недоступен"

- Убедитесь что backend запущен: `curl http://localhost:5000/`
- Проверьте URL в настройках
- Проверьте firewall

### Ошибка импорта GTK

```bash
# Ubuntu/Debian
sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0

# Fedora
sudo dnf install python3-gobject gtk3

# Arch
sudo pacman -S python-gobject gtk3
```

### Окно не перемещается

Это нормально на некоторых DE. Используйте Alt+перетаскивание или настройте позицию в config.json.

### Пустое расписание

- Проверьте настройку **Подгруппа** (попробуйте поставить 0)
- Убедитесь что вы авторизованы
- Обновите вручную (кнопка "Обновить")

## 🎨 Кастомизация

### Изменить размер окна

Отредактируйте `~/.config/timetable-widget/config.json`:
```json
{
  "width": 500,
  "height": 600
}
```

### Изменить прозрачность

Используйте настройки приложения или отредактируйте config.json:
```json
{
  "transparency": 0.9
}
```

### Изменить стили

Отредактируйте CSS в `timetable_widget/window.py` (функция `build_ui`).

## 🔐 Безопасность

- Пароль **не хранится** в приложении
- Токен хранится в backend в зашифрованном виде
- Все данные передаются через localhost
- Конфигурация хранится в `~/.config` с правами пользователя

## 📝 API Backend

Приложение общается с backend через REST API:

- `POST /api/auth/login` - Авторизация
- `GET /api/auth/status` - Проверка статуса
- `GET /api/timetable/today` - Расписание на сегодня
- `PUT /api/settings` - Сохранить настройки

## 🔄 Обновление

```bash
cd PythonGTK
git pull  # Если используете git
./install.sh
```

## 🗑️ Удаление

```bash
sudo rm -rf /usr/local/lib/timetable-widget
sudo rm /usr/local/bin/timetable-widget
rm ~/.local/share/applications/timetable-widget.desktop
rm -rf ~/.config/timetable-widget
```

## 👥 Авторы

- **Код виджета**: Казаков Кирилл Валерьевич, КТ-31-21
- **API расписания**: Петрянкин Даниил Евгеньевич, КТ-31-21
- **Дизайн**: Гаврилов Александр Сергеевич, КТ-42-20

## 📄 Лицензия

См. соглашения ЧувГУ:
- [Пользовательское соглашение](https://online.chuvsu.ru/doc/user_agreement)
- [Политика конфиденциальности](https://online.chuvsu.ru/doc/privacy_policy)

## 🆘 Поддержка

При проблемах:
1. Проверьте что backend запущен
2. Проверьте зависимости Python и GTK
3. Переустановите: `./install.sh`
4. Проверьте логи: запустите из терминала и смотрите вывод
